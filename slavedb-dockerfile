FROM mariadb:latest

# Copy your custom configuration file
COPY slave.cnf /etc/mysql/conf.d/slave.cnf
RUN chmod 644 /etc/mysql/conf.d/slave.cnf

# # Copy the entrypoint script into the container
# COPY slave-mdb-entrypoint.sh /usr/local/bin/slave-mdb-entrypoint.sh
# RUN chmod +x /usr/local/bin/slave-mdb-entrypoint.sh

# # Set the entrypoint script to be executed
# ENTRYPOINT ["/usr/local/bin/slave-mdb-entrypoint.sh"]

# By default, run MariaDB server
CMD ["mariadbd"]
